//! The editor's slot ids: the data container and the goal hand's goal.
//! Must match the <complication> ids in watchface.xml.
module FieldLocation {
    enum Value {
        CENTER = 1,
        GOAL = 2
    }
}
